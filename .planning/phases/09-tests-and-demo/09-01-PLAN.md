---
phase: 09-tests-and-demo
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/sample_transcript.json
  - tests/fixtures/sample_acc_event.json
  - tests/fixtures/expected_proposal.json
  - tests/unit/test_normalizer.py
  - tests/unit/test_router.py
autonomous: true
requirements:
  - TEST-01
  - TEST-03

must_haves:
  truths:
    - "All three fixture JSON files contain valid, parseable content that matches the demo window-substitution scenario"
    - "sample_transcript.json passes Fireflies webhook validation (_REQUIRED_KEYS intersection check)"
    - "expected_proposal.json fields match the Proposal Pydantic model exactly (proposal_id, event_id, alert, actions, confidence_score, recommendation)"
    - "Unit tests for normalizer and router modules run and pass with no skips (implementation files exist)"
    - "Running pytest tests/unit/ --cov=src --cov-fail-under=80 exits 0"
  artifacts:
    - path: "tests/fixtures/sample_transcript.json"
      provides: "Fireflies webhook demo payload"
      contains: "meetingId, transcript, meeting keys"
    - path: "tests/fixtures/sample_acc_event.json"
      provides: "ACC webhook demo payload"
      contains: "event_type, project_id, element keys"
    - path: "tests/fixtures/expected_proposal.json"
      provides: "Expected Proposal shape for pipeline assertion"
      contains: "proposal_id, event_id, alert, actions, confidence_score, recommendation"
    - path: "tests/unit/test_normalizer.py"
      provides: "Normalizer unit tests"
      min_lines: 60
    - path: "tests/unit/test_router.py"
      provides: "Router unit tests"
      min_lines: 60
  key_links:
    - from: "tests/fixtures/sample_transcript.json"
      to: "src/input/webhooks/fireflies.py"
      via: "_REQUIRED_KEYS intersection check"
      pattern: "meetingId|transcript|meeting|\\\"id\\\""
    - from: "tests/fixtures/expected_proposal.json"
      to: "src/shared/models/proposals.py"
      via: "Proposal field names"
      pattern: "proposal_id.*event_id.*alert.*actions.*confidence_score.*recommendation"
---

<objective>
Create all three demo fixture JSON files and fill coverage gaps in normalizer and router unit tests to reach the 80% coverage gate.

Purpose: Phase 9 cannot proceed to integration tests until fixtures exist (integration tests load them) and the coverage gate is green (phase success criterion 1).
Output: Three populated fixture files, additional normalizer/router tests, green coverage gate.
</objective>

<execution_context>
@C:/Users/Rafik/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Rafik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-tests-and-demo/09-RESEARCH.md

# Key source files for fixture shape and test patterns
@src/shared/models/proposals.py
@src/shared/models/events.py
@src/input/webhooks/fireflies.py
@tests/conftest.py
@tests/unit/test_normalizer.py
@tests/unit/test_router.py
</context>

<interfaces>
<!-- Key contracts the executor needs — extracted from codebase. -->

From src/shared/models/proposals.py:
```python
class Action(BaseModel):
    action_id: str  # uuid4
    action_type: Literal["email", "task", "calendar", "drawing"]
    action_data: dict
    status: Literal["pending", "executed", "failed"] = "pending"
    executed_at: Optional[datetime] = None

class Proposal(BaseModel):
    proposal_id: str  # uuid4
    event_id: str
    alert: dict          # {title, description, severity}
    actions: list[Action]
    confidence_score: float  # 0.0-1.0
    recommendation: Literal["accept", "review", "reject"]
    status: Literal["pending", "accepted", "rejected"] = "pending"
    created_at: datetime
```

From src/input/webhooks/fireflies.py:
```python
_REQUIRED_KEYS = {"transcript", "meeting", "meetingId", "id"}
# Validation: _REQUIRED_KEYS.intersection(body.keys()) must be non-empty
```

From tests/unit/test_normalizer.py (established pattern):
```python
_IMPL_PATH = Path(__file__).parent.parent.parent / "src/system/data_processing/normalizer.py"
IMPL_AVAILABLE = _IMPL_PATH.exists() and _IMPL_PATH.stat().st_size > 10
pytestmark = pytest.mark.skipif(not IMPL_AVAILABLE, reason="...")

@pytest.fixture(autouse=True)
def _setup_env(monkeypatch):
    monkeypatch.setenv("ANTHROPIC_API_KEY", "test-key")
    monkeypatch.setenv("VOYAGE_API_KEY", "test-key")
    monkeypatch.setenv("DATABASE_URL", "postgresql://test:test@localhost/test")
    for mod in ["src.system.data_processing.normalizer", "config.settings"]:
        sys.modules.pop(mod, None)
```

Demo scenario (from STATE.md):
- Window material: Aluminum -> Wood, 3rd Floor, 12 units W-301 to W-312
- confidence=60 on NormalizedEvent produces score=85.5 (~86%)
- Historical matches: similarities [0.92, 0.88, 0.90]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create all three demo fixture JSON files</name>
  <files>tests/fixtures/sample_transcript.json, tests/fixtures/sample_acc_event.json, tests/fixtures/expected_proposal.json</files>
  <action>
Create three fixture files for the window-substitution demo scenario. These files are currently empty (0 bytes).

**tests/fixtures/sample_transcript.json** — Fireflies demo payload.
Must contain at least one key from `_REQUIRED_KEYS = {"transcript", "meeting", "meetingId", "id"}` or the webhook returns 400. Include all four for maximum coverage:
```json
{
  "meetingId": "meeting-demo-001",
  "id": "transcript-demo-001",
  "meeting": {
    "title": "Construction Site Coordination — 3rd Floor Windows",
    "date": "2026-03-14T09:00:00Z"
  },
  "transcript": "We need to change the third-floor windows from aluminum to wood, 12 units W-301 to W-312. Jane from Premium Wood Co can supply at $320 per unit. Mike needs to approve procurement.",
  "attendees": [
    {"name": "John Smith", "role": "Project Manager"},
    {"name": "Jane Doe", "role": "Supplier Contact — Premium Wood Co"},
    {"name": "Mike Chen", "role": "Procurement Lead"}
  ]
}
```

**tests/fixtures/sample_acc_event.json** — ACC webhook demo payload for a drawing issue event:
```json
{
  "event_type": "issue.created",
  "project_id": "demo-project-acc-001",
  "element": {
    "element_id": "elem-W301",
    "element_type": "window",
    "location": "3rd Floor",
    "title": "Window Material Change Required — W-301 to W-312"
  },
  "payload": {
    "issue_id": "issue-acc-demo-001",
    "title": "Aluminum to Wood Window Substitution",
    "description": "Material change request for 12 window units on the 3rd floor",
    "assignee": "mike@site.com",
    "due_date": "2026-04-15T00:00:00Z"
  }
}
```

**tests/fixtures/expected_proposal.json** — Expected proposal shape. Fields must match `Proposal` model exactly (`proposal_id`, `event_id`, `alert`, `actions`, `confidence_score`, `recommendation`). Do NOT include `status` or `created_at` (those are generated at runtime; pipeline test does partial key matching, not full equality):
```json
{
  "proposal_id": "prop-demo-001",
  "event_id": "evt-demo-001",
  "alert": {
    "title": "Material Substitution: Aluminum to Wood Windows — 3rd Floor",
    "description": "12 window units (W-301 to W-312) require material substitution from aluminum to wood frames. Supplier: Premium Wood Co (Jane Doe). Procurement approval required.",
    "severity": "medium"
  },
  "actions": [
    {
      "action_type": "email",
      "action_data": {
        "to": "jane@premiumwood.com",
        "subject": "Wood Window Frame Order — 12 Units W-301 to W-312",
        "body": "Please provide a quote for 12 wood window frames for units W-301 to W-312, 3rd floor."
      }
    },
    {
      "action_type": "task",
      "action_data": {
        "title": "Procurement Approval: Wood Window Frames x12",
        "assignee": "mike@site.com",
        "due_date": "2026-03-21"
      }
    },
    {
      "action_type": "calendar",
      "action_data": {
        "summary": "Material Review: Aluminum to Wood Window Substitution",
        "start": "2026-03-18T10:00:00Z",
        "duration_minutes": 60
      }
    },
    {
      "action_type": "drawing",
      "action_data": {
        "drawing_number": "A-301",
        "annotation_text": "Material substitution: aluminum -> wood, 12 units W-301 to W-312"
      }
    }
  ],
  "confidence_score": 0.855,
  "recommendation": "accept"
}
```
  </action>
  <verify>
    <automated>python -c "import json, sys; [json.loads(open(f).read()) for f in ['tests/fixtures/sample_transcript.json', 'tests/fixtures/sample_acc_event.json', 'tests/fixtures/expected_proposal.json']]; print('All fixtures valid JSON')" && python -c "import json; d=json.load(open('tests/fixtures/sample_transcript.json')); assert {'transcript','meeting','meetingId','id'}.intersection(d.keys()), 'Missing required Fireflies key'; print('Fireflies keys OK')" && python -c "import json; d=json.load(open('tests/fixtures/expected_proposal.json')); required={'proposal_id','event_id','alert','actions','confidence_score','recommendation'}; assert required.issubset(d.keys()), f'Missing: {required-d.keys()}'; print('Proposal fixture keys OK')"</automated>
  </verify>
  <done>All three fixture files contain valid JSON. sample_transcript.json passes Fireflies key check. expected_proposal.json contains all required Proposal model fields. Actions list has all four action_types: email, task, calendar, drawing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill unit test coverage gaps for normalizer and router to reach 80% gate</name>
  <files>tests/unit/test_normalizer.py, tests/unit/test_router.py</files>
  <behavior>
    - test_normalizer: review_required is True when confidence field < 70 on NormalizedEvent
    - test_normalizer: NormalizedEvent with empty summary raises ValidationError
    - test_normalizer: normalize_event() with a patched LLM response returns a NormalizedEvent with correct material_original and material_new fields
    - test_router: route_event() falls back to "other" event_type when Haiku returns unrecognised classification
    - test_router: route_event() returns RoutedEvent with a loaded EventTypeConfig when classification is "material_change"
    - test_router: route_event() logs and falls back when YAML config file is missing
  </behavior>
  <action>
Read the existing tests/unit/test_normalizer.py and tests/unit/test_router.py to identify which behaviors are already tested before adding new tests. Do NOT duplicate existing tests.

**For test_normalizer.py** — run `python -m pytest tests/unit/test_normalizer.py --cov=src/system/data_processing/normalizer --cov-report=term-missing -q` first to see which lines are uncovered, then add tests for uncovered branches. Patterns to follow:
- Use the `_setup_env` autouse fixture (already in file) — do NOT add a duplicate fixture
- Use `patch.object(module, 'attr')` not string patches when sys.modules is popped
- Lazy-import `normalizer` module inside each new test function body (after `sys.modules.pop`)
- Use `pytest.mark.unit` marker on new tests

**For test_router.py** — same approach: read existing tests, run coverage, add tests for uncovered branches only. The router fallback chain (invalid Haiku output -> other.yaml, YAML load failure -> other.yaml) and config_dir injection are likely untested branches.

**Coverage target:** After adding tests, `python -m pytest tests/unit/ --cov=src --cov-fail-under=80 -q` must exit 0. Focus new tests on the files actually below 80% — do not rewrite already-passing tests.
  </action>
  <verify>
    <automated>cd C:/Users/Rafik/Documents/GitHub/Research-studio_GigAI && python -m pytest tests/unit/test_normalizer.py tests/unit/test_router.py -v -q 2>&1 | tail -20</automated>
  </verify>
  <done>All tests in test_normalizer.py and test_router.py pass (no failures, no errors). Running python -m pytest tests/unit/ --cov=src --cov-fail-under=80 -q exits with code 0.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `python -c "import json; [json.load(open(f)) for f in ['tests/fixtures/sample_transcript.json','tests/fixtures/sample_acc_event.json','tests/fixtures/expected_proposal.json']]; print('fixtures OK')"` — exits 0
2. `python -m pytest tests/unit/ -q` — all tests pass
3. `python -m pytest tests/unit/ --cov=src --cov-fail-under=80 -q` — exits 0 (coverage gate green)
</verification>

<success_criteria>
1. Three fixture files exist and contain valid JSON matching demo scenario shape
2. sample_transcript.json passes Fireflies webhook key validation
3. expected_proposal.json fields align exactly with Proposal Pydantic model
4. All unit tests in test_normalizer.py and test_router.py pass
5. pytest --cov=src --cov-fail-under=80 exits 0 for tests/unit/
</success_criteria>

<output>
After completion, create `.planning/phases/09-tests-and-demo/09-01-SUMMARY.md` with:
- Fixture files created and their key structure
- Coverage percentage reached
- Which specific normalizer/router tests were added
- Any coverage gaps that remain in other modules
</output>
