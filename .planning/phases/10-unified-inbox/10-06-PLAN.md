---
phase: 10-unified-inbox
plan: 06
type: execute
wave: 5
depends_on:
  - 10-05
files_modified:
  - dashboard/src/components/Sidebar.jsx
  - dashboard/src/App.jsx
autonomous: false
requirements:
  - INBOX-04

must_haves:
  truths:
    - "Sidebar has an 'Inbox' tab with an unread count badge that updates live"
    - "Clicking 'Inbox' in the sidebar shows the InboxFeed component in the main content area"
    - "The unread badge disappears (shows 0 or hides) when all items are read"
    - "All existing tabs (proposals, projects, employees, schedule) still work unchanged"
  artifacts:
    - path: "dashboard/src/components/Sidebar.jsx"
      provides: "Sidebar with Inbox tab entry including unread badge prop"
      exports: ["Sidebar"]
    - path: "dashboard/src/App.jsx"
      provides: "App wired to render InboxFeed when activeTab === 'inbox'"
      exports: ["default"]
  key_links:
    - from: "dashboard/src/App.jsx"
      to: "dashboard/src/components/InboxFeed.jsx"
      via: "conditional render {activeTab === 'inbox' && <InboxFeed />}"
      pattern: "activeTab.*inbox.*InboxFeed|InboxFeed.*activeTab.*inbox"
    - from: "dashboard/src/App.jsx"
      to: "dashboard/src/components/Sidebar.jsx"
      via: "inboxUnread prop passed to Sidebar"
      pattern: "inboxUnread"
---

<objective>
Wire the InboxFeed into the existing app navigation by adding an Inbox tab to the Sidebar and rendering InboxFeed in App.jsx.

Purpose: The PM needs to reach the inbox from the main navigation. This is the final wiring step that makes the unified inbox accessible in the live dashboard.

Output: Updated `Sidebar.jsx` (Inbox tab with unread badge) + updated `App.jsx` (InboxFeed import, tab case, unread state).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-unified-inbox/10-05-SUMMARY.md

<interfaces>
Current Sidebar.jsx (full file — read before editing):
```jsx
export function Sidebar({ activeTab, onTabChange }) {
  const tabs = [
    { id: 'proposals', label: 'Proposals', icon: '◈' },
    { id: 'projects',  label: 'Projects',  icon: '▦' },
    { id: 'employees', label: 'Team',      icon: '◉' },
    { id: 'schedule',  label: 'Schedule',  icon: '▸' },
  ]
  return (
    <nav className="sidebar">
      {tabs.map((tab) => (
        <button key={tab.id} className={`sidebar-tab ${activeTab === tab.id ? 'active' : ''}`}
          onClick={() => onTabChange(tab.id)}>
          <span className="sidebar-icon">{tab.icon}</span>
          <span className="sidebar-label">{tab.label}</span>
        </button>
      ))}
    </nav>
  )
}
```

Current App.jsx (full file — read before editing):
```jsx
// activeTab state, handles proposals/projects/employees/schedule cases
// Sidebar used as: <Sidebar activeTab={activeTab} onTabChange={setActiveTab} />
```

InboxFeed (Plan 05 output):
```jsx
// export function InboxFeed() { ... }
// No required props — manages its own data fetching
```

CSS design system variables (for badge):
```css
--accent: #0a84ff;
--red: #ff453a;
--text-primary: rgba(255,255,255,0.92);
--radius-sm: 8px;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Inbox tab with unread badge to Sidebar</name>
  <files>dashboard/src/components/Sidebar.jsx</files>
  <read_first>
    - dashboard/src/components/Sidebar.jsx — read the ENTIRE current file before making any changes
    - dashboard/src/index.css — search for ".sidebar-tab" to understand existing tab styles
  </read_first>
  <action>
    Update dashboard/src/components/Sidebar.jsx to:

    1. Accept a new prop: inboxUnread (number, default 0)

    2. Add inbox entry to the tabs array:
       { id: 'inbox', label: 'Inbox', icon: '⊞' }
       Position it as the SECOND entry (after 'proposals', before 'projects'), so the tab order becomes:
       proposals, inbox, projects, employees, schedule

    3. For the inbox tab button, add an unread badge next to the label:
       When rendering the tab with id === 'inbox', add a small badge showing inboxUnread count
       if inboxUnread > 0. Badge style:
         display: inline-flex, align-items: center, justify-content: center
         background: var(--red), color: #fff, border-radius: 50%
         min-width: 16px, height: 16px, font-size: 10px, font-weight: 600
         margin-left: 6px, padding: 0 4px

    The updated component signature:
      export function Sidebar({ activeTab, onTabChange, inboxUnread = 0 }) {

    All other tabs remain exactly as they were. Only add the inbox entry and badge rendering logic.
    Do not touch any CSS classes or other button markup.
  </action>
  <verify>
    <automated>cd "/c/Users/Rafik/OneDrive/Python Resources/testicule" && node -e "
const src = require('fs').readFileSync('dashboard/src/components/Sidebar.jsx','utf8');
console.assert(src.includes(\"id: 'inbox'\"), 'Missing inbox tab entry');
console.assert(src.includes('inboxUnread'), 'Missing inboxUnread prop');
console.assert(src.includes('sidebar-tab'), 'sidebar-tab class must be preserved');
const tabIds = src.match(/id: '[a-z]+'/g);
console.log('Tab IDs found:', tabIds);
console.assert(tabIds && tabIds.length === 5, 'Expected 5 tabs');
console.log('Sidebar assertions PASS');
"</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar accepts inboxUnread prop with default 0
    - tabs array has exactly 5 entries: proposals, inbox, projects, employees, schedule (in that order)
    - Inbox badge renders with var(--red) background when inboxUnread > 0
    - All existing tabs preserve their id, label, icon, and className patterns
  </acceptance_criteria>
  <done>Sidebar has 5 tabs with inbox second. Badge shows inboxUnread count in red. Existing tab structure unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire InboxFeed into App.jsx with unread state tracking</name>
  <files>dashboard/src/App.jsx</files>
  <read_first>
    - dashboard/src/App.jsx — read the ENTIRE current file before making changes
    - dashboard/src/components/InboxFeed.jsx — confirm the exported function name is InboxFeed and it takes no required props
    - dashboard/src/api.js — read fetchInboxStats to understand return shape ({total, unread, ...})
  </read_first>
  <action>
    Update dashboard/src/App.jsx with the following minimal additions (do NOT reorganize or remove any existing code):

    1. Add import at top:
       import { InboxFeed } from './components/InboxFeed.jsx'
       import { fetchInboxStats } from './api.js'

    2. Add state for inbox unread count:
       const [inboxUnread, setInboxUnread] = useState(0)

    3. Add a useEffect that loads inbox unread count on mount and refreshes every 30 seconds:
       useEffect(() => {
         async function loadUnread() {
           const stats = await fetchInboxStats()
           if (stats) setInboxUnread(stats.unread ?? 0)
         }
         loadUnread()
         const t = setInterval(loadUnread, 30000)
         return () => clearInterval(t)
       }, [])

    4. Pass inboxUnread to Sidebar:
       Change: <Sidebar activeTab={activeTab} onTabChange={setActiveTab} />
       To:     <Sidebar activeTab={activeTab} onTabChange={setActiveTab} inboxUnread={inboxUnread} />

    5. Add inbox tab case inside the main-content div (add AFTER the schedule case, following the same pattern):
       {activeTab === 'inbox' && (
         <main className="page-panel">
           <InboxFeed />
         </main>
       )}

    Keep ALL existing code untouched. This is purely additive.
  </action>
  <verify>
    <automated>cd "/c/Users/Rafik/OneDrive/Python Resources/testicule" && node -e "
const src = require('fs').readFileSync('dashboard/src/App.jsx','utf8');
console.assert(src.includes('InboxFeed'), 'Missing InboxFeed import/usage');
console.assert(src.includes('inboxUnread'), 'Missing inboxUnread state');
console.assert(src.includes(\"activeTab === 'inbox'\"), 'Missing inbox tab case');
console.assert(src.includes('fetchInboxStats'), 'Missing fetchInboxStats call');
console.assert(src.includes('ProposalFeed'), 'ProposalFeed must still be present (existing)');
console.assert(src.includes('ProjectLibrary'), 'ProjectLibrary must still be present (existing)');
console.log('App.jsx assertions PASS');
"</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx imports InboxFeed and fetchInboxStats
    - inboxUnread state initialized to 0, loaded from /api/inbox/stats on mount
    - inboxUnread refreshes every 30 seconds via setInterval
    - Sidebar receives inboxUnread prop
    - {activeTab === 'inbox'} case renders InboxFeed inside page-panel main
    - All 4 existing tab cases (proposals, projects, employees, schedule) still present and unchanged
  </acceptance_criteria>
  <done>App.jsx wired. Inbox tab renders InboxFeed. Unread count loads from API and refreshes every 30s. All existing tabs untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 10 Unified Inbox:
    - Backend: InboxItem models, classifier, 55+ seed items, 5 API endpoints (/api/inbox, /api/inbox/stats, /api/inbox/{id}, /api/inbox/{id}/read, /api/inbox/ingest)
    - Frontend: InboxFeed with filter bar + stats bar, InboxItem expandable cards, Sidebar Inbox tab with unread badge
  </what-built>
  <how-to-verify>
    1. Start the backend: cd to project root, run uvicorn main:app --reload
    2. Start the frontend: cd dashboard, run npm run dev
    3. Open http://localhost:5173 in browser
    4. Verify: "Inbox" tab appears in sidebar (second position, after Proposals)
    5. Click Inbox tab — verify inbox feed loads with multiple items
    6. Verify stats bar shows total count and unread count
    7. Verify filter dropdowns: source, comm_type, urgency — apply a filter and confirm list updates
    8. Click an unread item — verify it expands showing action items and sender info
    9. Verify the unread badge in sidebar decreases after reading items
    10. For an item with a source_url, verify "Open Source" button is visible in expanded view
    11. Check at least one item from each source type appears (gmail=blue, fireflies=purple, acc=orange, internal=gray)
    12. Check at least one escalation item appears with red left border
  </how-to-verify>
  <resume-signal>Type "approved" if inbox works end-to-end, or describe any specific issues</resume-signal>
</task>

</tasks>

<verification>
Static check (no server needed):
cd "/c/Users/Rafik/OneDrive/Python Resources/testicule" && node -e "
const appSrc = require('fs').readFileSync('dashboard/src/App.jsx','utf8');
const sidebarSrc = require('fs').readFileSync('dashboard/src/components/Sidebar.jsx','utf8');
['InboxFeed','inboxUnread','inbox'].forEach(s => console.assert(appSrc.includes(s), 'App.jsx missing: ' + s));
['inbox','inboxUnread'].forEach(s => console.assert(sidebarSrc.includes(s), 'Sidebar.jsx missing: ' + s));
console.log('PASS');
"
</verification>

<success_criteria>
- Sidebar shows Inbox tab in second position with red unread badge when count > 0
- App.jsx renders InboxFeed when activeTab === 'inbox'
- Unread count refreshes from API every 30 seconds
- All existing tabs (proposals, projects, team, schedule) unaffected
- Human verification confirms full end-to-end flow works in browser
</success_criteria>

<output>
After completion, create .planning/phases/10-unified-inbox/10-06-SUMMARY.md
</output>
