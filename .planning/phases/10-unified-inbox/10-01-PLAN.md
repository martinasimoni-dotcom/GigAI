---
phase: 10-unified-inbox
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/inbox/__init__.py
  - src/inbox/models.py
  - src/inbox/store.py
autonomous: true
requirements:
  - INBOX-01
  - INBOX-02
  - INBOX-03
  - INBOX-05
  - INBOX-06

must_haves:
  truths:
    - "An InboxItem Pydantic model exists with all fields needed to represent a communication from any source"
    - "InboxItem has source, comm_type, urgency, action_items, project_id, and source_url fields"
    - "An in-memory store provides thread-safe add/list/get operations for InboxItems"
    - "The store supports filtering by project_id, comm_type, and urgency range"
  artifacts:
    - path: "src/inbox/__init__.py"
      provides: "Package init"
    - path: "src/inbox/models.py"
      provides: "InboxItem, ActionItem, CommType, SourceType Pydantic v2 models"
      exports: ["InboxItem", "ActionItem", "CommType", "SourceType"]
    - path: "src/inbox/store.py"
      provides: "InboxStore — thread-safe in-memory CRUD with filtering"
      exports: ["InboxStore", "inbox_store"]
  key_links:
    - from: "src/inbox/store.py"
      to: "src/inbox/models.py"
      via: "InboxItem import"
      pattern: "from src.inbox.models import InboxItem"
---

<objective>
Define the InboxItem data model and in-memory store that all other Phase 10 plans depend on.

Purpose: Every component in the unified inbox (classifier, seed, API, frontend) needs a shared contract for what an inbox communication looks like. This plan establishes that contract first so downstream plans can build against concrete types.

Output: `src/inbox/models.py` (Pydantic v2 models) + `src/inbox/store.py` (in-memory store singleton).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing patterns to follow exactly -->

From src/shared/models/events.py:
```python
# Pydantic v2 style used throughout — follow this exact pattern
from pydantic import BaseModel, ConfigDict, Field, field_validator
class RawEvent(BaseModel):
    model_config = ConfigDict(str_strip_whitespace=True)
    event_id: str
    source: Literal["fireflies", "acc", "gmail", "calendar"]
    raw_payload: dict
    received_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))
```

From src/data/projects.py (Project model pattern):
```python
class Project(BaseModel):
    project_id: str
    name: str
    # ... fields use Field() with ge/le constraints where needed
    priority: str = "medium"  # default values inline
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define InboxItem and related Pydantic v2 models</name>
  <files>src/inbox/__init__.py, src/inbox/models.py</files>
  <read_first>
    - src/shared/models/events.py — follow Pydantic v2 ConfigDict/field_validator patterns exactly
    - src/data/projects.py — follow Field() constraint patterns
  </read_first>
  <behavior>
    - CommType literal: "decision", "action-item", "FYI", "question", "escalation"
    - SourceType literal: "gmail", "fireflies", "acc", "internal"
    - UrgencyLevel: int 1-5 (1=low, 5=critical)
    - ActionItem: assignee: str, deadline: Optional[datetime], priority: Literal["low","medium","high"], description: str
    - InboxItem fields: item_id (str uuid4 default), source (SourceType), comm_type (CommType), urgency (int 1-5), summary (str non-empty), raw_text (str), project_id (Optional[str]), project_name (Optional[str]), sender (str), sender_email (Optional[str]), subject (Optional[str]), source_url (Optional[str]), action_items (list[ActionItem] default=[]), received_at (datetime utc default), classified_at (Optional[datetime]), is_read (bool default False)
    - field_validator on summary: must not be empty
    - field_validator on urgency: ge=1, le=5
  </behavior>
  <action>
    Create src/inbox/__init__.py as empty file (just a docstring: "Unified Inbox package.").

    Create src/inbox/models.py:
    - Import: datetime, timezone, Optional, Literal, TYPE_CHECKING from standard library
    - Import: BaseModel, ConfigDict, Field, field_validator from pydantic
    - Define CommType as Literal["decision", "action-item", "FYI", "question", "escalation"]
    - Define SourceType as Literal["gmail", "fireflies", "acc", "internal"]
    - Define ActionItem(BaseModel) with model_config = ConfigDict(str_strip_whitespace=True):
        assignee: str
        deadline: Optional[datetime] = None
        priority: Literal["low", "medium", "high"] = "medium"
        description: str
    - Define InboxItem(BaseModel) with model_config = ConfigDict(str_strip_whitespace=True):
        item_id: str = Field(default_factory=lambda: str(uuid.uuid4()))
        source: SourceType
        comm_type: CommType
        urgency: int = Field(ge=1, le=5, default=3)
        summary: str
        raw_text: str
        project_id: Optional[str] = None
        project_name: Optional[str] = None
        sender: str
        sender_email: Optional[str] = None
        subject: Optional[str] = None
        source_url: Optional[str] = None
        action_items: list[ActionItem] = Field(default_factory=list)
        received_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))
        classified_at: Optional[datetime] = None
        is_read: bool = False
    - Add field_validator("summary", mode="after") @classmethod that raises ValueError if not v.strip()
    - Import uuid at top of file

    Write tests in src/inbox/tests/test_models.py:
    - test_inbox_item_defaults: creates InboxItem with minimal fields, verifies item_id is generated, is_read=False, urgency=3
    - test_summary_validator_rejects_empty: expects ValueError on InboxItem(summary="  ", ...)
    - test_action_item_priority_default: ActionItem has priority="medium" by default
    - test_urgency_bounds: urgency=0 raises ValidationError, urgency=6 raises ValidationError
  </action>
  <verify>
    <automated>cd "/c/Users/Rafik/OneDrive/Python Resources/testicule" && python -m pytest src/inbox/tests/test_models.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 model tests pass. src/inbox/models.py exports InboxItem, ActionItem, CommType, SourceType with correct Pydantic v2 patterns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build thread-safe InboxStore singleton</name>
  <files>src/inbox/store.py</files>
  <read_first>
    - src/inbox/models.py — (just created) for InboxItem type
    - src/api/routes.py — see how _proposals list + dict pattern works (lines 31-32) — InboxStore follows same in-memory approach
  </read_first>
  <behavior>
    - add(item: InboxItem) -> None: appends to internal list, thread-safe via threading.Lock
    - get(item_id: str) -> Optional[InboxItem]: O(n) lookup by item_id
    - list_items(project_id=None, comm_type=None, min_urgency=1, max_urgency=5, unread_only=False, limit=100, offset=0) -> tuple[list[InboxItem], int]: returns (page, total_count)
    - mark_read(item_id: str) -> bool: sets is_read=True, returns True if found
    - clear() -> None: empties store (for tests/seed reload)
    - inbox_store module-level singleton = InboxStore()
  </behavior>
  <action>
    Create src/inbox/store.py:
    - Import: threading, Optional, TYPE_CHECKING
    - TYPE_CHECKING guard: from src.inbox.models import InboxItem
    - If not TYPE_CHECKING: use string annotations or lazy import inside methods

    Actually: use a direct top-level import (no TYPE_CHECKING needed here — store.py is not in a circular import path with models.py):
      from src.inbox.models import InboxItem

    Class InboxStore:
      def __init__(self):
          self._items: list[InboxItem] = []
          self._lock = threading.Lock()

      def add(self, item: "InboxItem") -> None:
          with self._lock:
              self._items.append(item)

      def get(self, item_id: str) -> "Optional[InboxItem]":
          with self._lock:
              return next((i for i in self._items if i.item_id == item_id), None)

      def list_items(self, project_id=None, comm_type=None, min_urgency=1,
                     max_urgency=5, unread_only=False, limit=100, offset=0):
          with self._lock:
              results = list(self._items)
          if project_id:
              results = [i for i in results if i.project_id == project_id]
          if comm_type:
              results = [i for i in results if i.comm_type == comm_type]
          results = [i for i in results if min_urgency <= i.urgency <= max_urgency]
          if unread_only:
              results = [i for i in results if not i.is_read]
          # Sort by urgency desc, then received_at desc
          results.sort(key=lambda x: (-x.urgency, -x.received_at.timestamp()))
          total = len(results)
          return results[offset:offset + limit], total

      def mark_read(self, item_id: str) -> bool:
          with self._lock:
              for item in self._items:
                  if item.item_id == item_id:
                      # Pydantic v2 model_copy for immutable update
                      idx = self._items.index(item)
                      self._items[idx] = item.model_copy(update={"is_read": True})
                      return True
          return False

      def clear(self) -> None:
          with self._lock:
              self._items.clear()

    # Module-level singleton
    inbox_store = InboxStore()

    Write tests in src/inbox/tests/test_store.py:
    - test_add_and_get: add item, get by item_id returns same item
    - test_list_filter_project: add 3 items with different project_ids, filter by one project_id returns only matching items
    - test_list_filter_comm_type: filter by comm_type="question" returns only questions
    - test_list_filter_urgency: min_urgency=4 returns only items with urgency >= 4
    - test_mark_read: mark_read returns True, subsequent get shows is_read=True
    - test_mark_read_not_found: mark_read on unknown id returns False
    - test_list_sorted_urgency: items returned sorted urgency desc
    - test_clear: after clear(), list_items returns empty list
  </action>
  <verify>
    <automated>cd "/c/Users/Rafik/OneDrive/Python Resources/testicule" && python -m pytest src/inbox/tests/test_store.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All 8 store tests pass. inbox_store singleton importable from src.inbox.store. Filtering and sorting work correctly.</done>
</task>

</tasks>

<verification>
Run all inbox model and store tests together:
cd "/c/Users/Rafik/OneDrive/Python Resources/testicule" && python -m pytest src/inbox/tests/ -x -q

Expected: 12 tests passing, 0 failures.

Also verify imports work:
python -c "from src.inbox.models import InboxItem, ActionItem; from src.inbox.store import inbox_store; print('OK')"
</verification>

<success_criteria>
- src/inbox/models.py: InboxItem and ActionItem with Pydantic v2 patterns, field validators, correct Literal types
- src/inbox/store.py: InboxStore with thread-safe CRUD, multi-field filtering, urgency+recency sort
- All 12 tests pass
- No circular imports, no module-level settings access
</success_criteria>

<output>
After completion, create .planning/phases/10-unified-inbox/10-01-SUMMARY.md
</output>
