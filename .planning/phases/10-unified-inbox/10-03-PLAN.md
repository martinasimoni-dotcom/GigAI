---
phase: 10-unified-inbox
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/inbox/seed.py
autonomous: true
requirements:
  - INBOX-01
  - INBOX-04

must_haves:
  truths:
    - "A seed script creates 55+ realistic InboxItems covering all 4 source types and all 5 comm_types"
    - "Items span multiple projects from the existing 30-project catalog"
    - "Urgency is distributed realistically: mostly 2-3, some 4-5 critical items"
    - "Each item has realistic action_items (some have 0, some have 2-3)"
    - "Calling seed_inbox() populates inbox_store with all 55+ items"
    - "Seed items have realistic source_urls pointing to plausible external resources"
  artifacts:
    - path: "src/inbox/seed.py"
      provides: "seed_inbox() — populates inbox_store with 55+ realistic InboxItems"
      exports: ["seed_inbox"]
  key_links:
    - from: "src/inbox/seed.py"
      to: "src/inbox/store.py"
      via: "inbox_store.add(item)"
      pattern: "inbox_store\\.add"
---

<objective>
Generate 55+ realistic dummy InboxItems that make the UI immediately useful for demo and development.

Purpose: The frontend and API need populated data to render and test against. This plan creates a deterministic seed function that populates the InboxStore with realistic construction project communications across all source types, comm_types, and urgency levels.

Output: `src/inbox/seed.py` with a `seed_inbox()` function returning the count of seeded items.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-unified-inbox/10-01-SUMMARY.md

<interfaces>
From src/inbox/models.py (Plan 01 output):
```python
InboxItem(
    source="gmail",               # SourceType: gmail|fireflies|acc|internal
    comm_type="action-item",      # CommType: decision|action-item|FYI|question|escalation
    urgency=4,                    # int 1-5
    summary="...",                # non-empty string
    raw_text="...",               # full communication body
    project_id="proj-001",        # from 30-project catalog
    project_name="Skyline Tower Phase 2",
    sender="Marcus Johnson",
    sender_email="marcus.johnson@contractor.com",
    subject="RE: Steel delivery delay",
    source_url="https://mail.google.com/mail/u/0/#inbox/abc123",
    action_items=[
        ActionItem(
            assignee="Sarah Chen",
            deadline=datetime(2026, 3, 25, tzinfo=timezone.utc),
            priority="high",
            description="Confirm revised delivery schedule with supplier"
        )
    ],
    received_at=datetime(2026, 3, 20, 9, 15, tzinfo=timezone.utc),
    classified_at=datetime(2026, 3, 20, 9, 15, 5, tzinfo=timezone.utc),
    is_read=False,
)
```

Project IDs and names to use (distribute evenly):
```
proj-001: Skyline Tower Phase 2
proj-002: Harbor View Residential Complex
proj-003: Metro Transit Hub
proj-004: Riverside Office Park
proj-005: Downtown Mixed-Use Development
proj-006: Industrial Logistics Center
proj-007: University Science Building
proj-008: Airport Terminal Expansion
proj-009: Medical Center Addition
proj-010: Coastal Resort Hotel
```

Employee names for senders and assignees:
```
Marcus Johnson, Sarah Chen, David Rodriguez, Emily Watson,
James Okafor, Priya Patel, Robert Kim, Linda Thompson,
Carlos Mendez, Aisha Williams
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write seed_inbox() with 55+ realistic InboxItems</name>
  <files>src/inbox/seed.py</files>
  <read_first>
    - src/inbox/models.py — read InboxItem and ActionItem fields exactly before writing any object construction
    - src/inbox/store.py — read inbox_store.add() and inbox_store.clear() signatures
    - src/data/projects.py — skim first 60 lines to confirm project_id format is "proj-001" etc.
  </read_first>
  <action>
    Create src/inbox/seed.py.

    Imports needed:
      from datetime import datetime, timedelta, timezone
      from src.inbox.models import InboxItem, ActionItem

    Define a base date: BASE = datetime(2026, 3, 20, 12, 0, tzinfo=timezone.utc)

    Build 55 InboxItems distributed as follows. Each block must use real construction language in raw_text (3-8 sentences each).

    GMAIL items (15) — source_url format "https://mail.google.com/mail/u/0/#inbox/{8-char-hex}":

    1. source="gmail", comm_type="escalation", urgency=5, project="proj-001", sender="Marcus Johnson",
       subject="URGENT: Stop Work Order - Structural Concern",
       raw_text: "The structural engineer from Jensen & Associates reviewed the anchor bolt placement during today's site visit and identified a critical deviation from the approved drawings on gridlines B-4 through B-7. The current placement is 3 inches off from the approved layout which exceeds the 1/4 inch tolerance specified in section 05 12 00 of the project specifications. We are issuing a stop work order on all structural steel erection until this is resolved. The affected area covers approximately 2,400 square feet of the fourth floor framing. All workers in the affected zone have been notified and cleared. We need an emergency meeting with the structural engineer, PM, and superintendent by 8 AM tomorrow.",
       summary="Stop work order issued on structural steel erection at Skyline Tower due to anchor bolt placement deviation exceeding specification tolerances.",
       action_items=[ActionItem(assignee="Sarah Chen", deadline=BASE-timedelta(days=0,hours=3), priority="high", description="Schedule emergency meeting with structural engineer by 8 AM tomorrow"), ActionItem(assignee="David Rodriguez", deadline=BASE-timedelta(days=0,hours=6), priority="high", description="Document all affected areas and photograph anchor bolt deviations")]
       received_at=BASE-timedelta(hours=4), is_read=False

    2. source="gmail", comm_type="action-item", urgency=4, project="proj-002",
       subject="RE: Steel Delivery Delay - Harbor View", sender="Carlos Mendez"
       raw_text about delivery pushed 2 weeks, impact on schedule, need expedited options
       1-2 action items about confirming delivery date and updating schedule

    3. source="gmail", comm_type="decision", urgency=3, project="proj-003", sender="Linda Thompson"
       subject="RE: Material Substitution Approval - Metro Transit Hub"
       raw_text about owner approving aluminum instead of steel for canopy
       1 action item about updating spec book

    4-15: Continue with the remaining 12 gmail items covering: RFI response required (proj-004, urgency 4), payment dispute (proj-005, urgency 4), weekly progress summary (proj-001, urgency 2, FYI, no action items), weather hold (proj-006, urgency 2, FYI), inspection passed (proj-007, urgency 1, FYI), spec clarification question (proj-008, urgency 3), LEED query (proj-009, urgency 2), permit timeline question (proj-010, urgency 3), vendor selection decision (proj-002, urgency 3), subcontractor payment decision (proj-003, urgency 3), safety incident escalation (proj-004, urgency 5).

    FIREFLIES items (15) — source_url format "https://app.fireflies.ai/view/meeting-{6-digit-id}":

    16-30: OAC meeting decisions (proj-001, urgency 4, decision), design RFI resolved in call (proj-002, urgency 3, decision), owner approval of change order (proj-003, urgency 3, decision), site visit followup (proj-004, urgency 3, action-item), drawing revision required (proj-005, urgency 4, action-item), punch list items assigned (proj-006, urgency 3, action-item), coordination call notes (proj-007, urgency 2, FYI), subcontractor kickoff summary (proj-008, urgency 2, FYI), weekly coordination (proj-009, urgency 1, FYI), fire suppression routing question (proj-010, urgency 3, question), MEP coordination question (proj-001, urgency 2, question), structural engineer flagged issue (proj-002, urgency 5, escalation), owner expressing cost concern (proj-003, urgency 4, escalation), schedule impact discussion (proj-004, urgency 4, decision), concrete pour decision (proj-005, urgency 3, decision).

    ACC items (15) — source_url format "https://acc.autodesk.com/projects/{proj-id}/issues/{4-digit-num}":

    31-45: RFI overdue (proj-006, urgency 5, escalation), safety observation critical (proj-007, urgency 5, escalation), document submittal rejected (proj-008, urgency 4, escalation), safety observation moderate (proj-009, urgency 4, escalation), punch list item (proj-010, urgency 3, action-item), inspection request (proj-001, urgency 3, action-item), drawing revision required (proj-002, urgency 4, action-item), material approval action (proj-003, urgency 3, action-item), document published (proj-004, urgency 1, FYI), inspection passed (proj-005, urgency 1, FYI), submittal approved (proj-006, urgency 2, FYI), RFI acknowledged (proj-007, urgency 2, FYI), spec interpretation question (proj-008, urgency 3, question), material approval status (proj-009, urgency 2, question), shop drawing query (proj-010, urgency 3, question).

    INTERNAL items (10) — source_url=None, sender_email=None:

    46-55: PM budget re-allocation decision (proj-001, urgency 3, decision), change order approval decision (proj-002, urgency 3, decision), resource re-assignment decision (proj-003, urgency 3, decision), team task - submit RFI response (proj-004, urgency 4, action-item), team task - schedule concrete inspection (proj-005, urgency 3, action-item), team task - update schedule baseline (proj-006, urgency 3, action-item), system notification - pipeline processed 3 events (proj-007, urgency 1, FYI), daily digest reminder (proj-008, urgency 1, FYI), team question about overtime approval (proj-009, urgency 2, question), team question about subcontractor scope (proj-010, urgency 2, question).

    Set received_at for older items: items 1-20 received_at = BASE - timedelta(days=N) where N ranges 0-19; items 21-40 received_at = BASE - timedelta(days=N) where N ranges 3-22; items 41-55 received_at = BASE - timedelta(days=N).
    Set is_read=True for items where (BASE - received_at).days >= 7, else False.
    Set classified_at = received_at + timedelta(seconds=5) for all items.

    Function structure:
      def _build_items() -> list[InboxItem]:
          items = []
          # ... all 55 InboxItem() constructions
          return items

      def seed_inbox(clear_first: bool = True) -> int:
          from src.inbox.store import inbox_store
          if clear_first:
              inbox_store.clear()
          items = _build_items()
          for item in items:
              inbox_store.add(item)
          return len(items)

      if __name__ == "__main__":
          count = seed_inbox()
          print(f"Seeded {count} inbox items")
  </action>
  <verify>
    <automated>cd "/c/Users/Rafik/OneDrive/Python Resources/testicule" && python -c "
from src.inbox.seed import seed_inbox
count = seed_inbox()
assert count >= 55, f'Expected >= 55 items, got {count}'
from src.inbox.store import inbox_store
items, total = inbox_store.list_items(limit=200)
sources = {i.source for i in items}
assert sources == {'gmail','fireflies','acc','internal'}, f'Missing sources: {sources}'
comm_types = {i.comm_type for i in items}
assert len(comm_types) == 5, f'Missing comm_types, got: {comm_types}'
urgencies = {i.urgency for i in items}
assert urgencies.issuperset({1,2,3,4,5}), f'Missing urgency levels: {urgencies}'
print(f'Seeded: {count}, Sources: {sources}, CommTypes: {comm_types}, PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - seed_inbox() returns >= 55
    - All 4 source types present: gmail, fireflies, acc, internal
    - All 5 comm_types present: decision, action-item, FYI, question, escalation
    - All 5 urgency levels (1-5) represented
    - Items with received_at >= 7 days ago have is_read=True
    - classified_at is set (5 seconds after received_at)
    - No module-level side effects — seed_inbox() must be called explicitly
  </acceptance_criteria>
  <done>Seed script runs without error, all assertions pass, store is populated with 55+ diverse realistic items.</done>
</task>

</tasks>

<verification>
cd "/c/Users/Rafik/OneDrive/Python Resources/testicule" && python src/inbox/seed.py
Expected output: "Seeded 55 inbox items" (or higher)
</verification>

<success_criteria>
- 55+ InboxItems created covering all sources, comm_types, and urgency levels
- Realistic construction language in raw_text (not Lorem Ipsum)
- No import-time side effects — only run on explicit call
- Older items marked is_read=True
- All items have classified_at set
</success_criteria>

<output>
After completion, create .planning/phases/10-unified-inbox/10-03-SUMMARY.md
</output>
