---
phase: 05-domain-processing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/event_types/material_change.yaml
  - config/event_types/schedule_update.yaml
  - config/event_types/rfi_request.yaml
autonomous: true
requirements:
  - DOM-01
  - DOM-02

must_haves:
  truths:
    - "material_change.yaml loads into EventTypeConfig without ValidationError"
    - "material_change.yaml has exactly 3 allowed_signals: material_order_required, schedule_update_needed, drawing_markup_required"
    - "material_change.yaml has at least 4 enrichment_queries covering lead time and structural context"
    - "schedule_update.yaml loads into EventTypeConfig without ValidationError"
    - "rfi_request.yaml loads into EventTypeConfig without ValidationError"
  artifacts:
    - path: "config/event_types/material_change.yaml"
      provides: "Full event type config for demo scenario"
      contains: "allowed_signals"
    - path: "config/event_types/schedule_update.yaml"
      provides: "Stub config for schedule_update event type"
      contains: "event_type: schedule_update"
    - path: "config/event_types/rfi_request.yaml"
      provides: "Stub config for rfi_request event type"
      contains: "event_type: rfi_request"
  key_links:
    - from: "config/event_types/material_change.yaml"
      to: "src/shared/models/config.py:EventTypeConfig"
      via: "yaml.safe_load + EventTypeConfig(**yaml_data)"
      pattern: "event_type|rules_file|allowed_signals"
    - from: "config/event_types/material_change.yaml"
      to: "knowledge_folder/rules/demo_project.yaml"
      via: "rules_file field"
      pattern: "rules_file: knowledge_folder/rules/demo_project.yaml"
---

<objective>
Expand material_change.yaml from its Phase 3 stub to the full config the domain processor needs, and verify that schedule_update.yaml and rfi_request.yaml load cleanly against the EventTypeConfig schema.

Purpose: The domain processor (Plan 04) reads EventTypeConfig to know which rules file to load, which signals are permitted, and which enrichment queries drove the context. A thin stub risks producing wrong signals in the demo scenario.

Output: Three YAML files that all instantiate successfully as EventTypeConfig objects.
</objective>

<execution_context>
@C:/Users/Rafik/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Rafik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-domain-processing/05-CONTEXT.md
@src/shared/models/config.py
@config/event_types/material_change.yaml
@config/event_types/schedule_update.yaml
@config/event_types/rfi_request.yaml

<interfaces>
<!-- EventTypeConfig schema — executor must produce YAML that satisfies all fields -->
From src/shared/models/config.py:
```python
class EventTypeConfig(BaseModel):
    event_type: str
    rules_file: str
    allowed_signals: list[str] = Field(default_factory=list)
    enrichment_queries: list[str] = Field(default_factory=list)
    time_analysis_enabled: bool = False
```

Current material_change.yaml (Phase 3 stub):
```yaml
event_type: material_change
rules_file: knowledge_folder/rules/demo_project.yaml
allowed_signals:
  - material_order_required
  - schedule_update_needed
  - drawing_markup_required
enrichment_queries:
  - "material supplier and pricing"
  - "past material change approvals"
time_analysis_enabled: true
```

Existing schedule_update.yaml:
```yaml
event_type: schedule_update
rules_file: knowledge_folder/rules/demo_project.yaml
allowed_signals:
  - schedule_update_needed
enrichment_queries: []
time_analysis_enabled: true
```

Existing rfi_request.yaml:
```yaml
event_type: rfi_request
rules_file: knowledge_folder/rules/demo_project.yaml
allowed_signals: []
enrichment_queries: []
time_analysis_enabled: false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand material_change.yaml to full config</name>
  <files>config/event_types/material_change.yaml</files>
  <action>
Overwrite config/event_types/material_change.yaml with the expanded version. Keep all existing fields, expand enrichment_queries, and add a comment block:

```yaml
# material_change.yaml — Full config for domain processing (DOM-01)
# Expanded in Phase 5 from Phase 3 stub.
event_type: material_change
rules_file: knowledge_folder/rules/demo_project.yaml
allowed_signals:
  - material_order_required
  - schedule_update_needed
  - drawing_markup_required
enrichment_queries:
  - "material supplier and pricing"
  - "past material change approvals"
  - "lead time window installation schedule"
  - "structural weight load bearing assessment"
time_analysis_enabled: true
```

The two new enrichment_queries support the demo scenario:
- "lead time window installation schedule" — surfaces installation events that the time analysis module will cross-reference for conflicts.
- "structural weight load bearing assessment" — pulls RULE-008 context (aluminum → wood, quantity > 8 triggers structural review).

Do NOT add keys beyond what EventTypeConfig defines (event_type, rules_file, allowed_signals, enrichment_queries, time_analysis_enabled) — extra YAML keys would cause a ValidationError because EventTypeConfig does not have extra="allow".
  </action>
  <verify>
    <automated>python -c "
import yaml
from src.shared.models.config import EventTypeConfig
with open('config/event_types/material_change.yaml') as f:
    data = yaml.safe_load(f)
cfg = EventTypeConfig(**data)
assert cfg.event_type == 'material_change'
assert 'material_order_required' in cfg.allowed_signals
assert 'schedule_update_needed' in cfg.allowed_signals
assert 'drawing_markup_required' in cfg.allowed_signals
assert len(cfg.enrichment_queries) >= 4
assert cfg.time_analysis_enabled is True
print('material_change.yaml OK — signals:', cfg.allowed_signals)
print('enrichment_queries:', cfg.enrichment_queries)
"</automated>
  </verify>
  <done>material_change.yaml loads into EventTypeConfig, has 3 allowed_signals and at least 4 enrichment_queries, time_analysis_enabled is true.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and improve schedule_update.yaml and rfi_request.yaml</name>
  <files>config/event_types/schedule_update.yaml, config/event_types/rfi_request.yaml</files>
  <action>
Both YAML stubs already exist from Phase 3. Verify they load cleanly. If the load verification below passes without changes, leave them as-is (they satisfy DOM-02 as stubs). If not, fix only what is needed.

Expected content for schedule_update.yaml (improve enrichment_queries if currently empty):
```yaml
# schedule_update.yaml — Event type config stub (DOM-02)
event_type: schedule_update
rules_file: knowledge_folder/rules/demo_project.yaml
allowed_signals:
  - schedule_update_needed
enrichment_queries:
  - "schedule delay impact critical path"
time_analysis_enabled: true
```

Expected content for rfi_request.yaml (add at least one allowed_signal):
```yaml
# rfi_request.yaml — Event type config stub (DOM-02)
event_type: rfi_request
rules_file: knowledge_folder/rules/demo_project.yaml
allowed_signals:
  - drawing_markup_required
enrichment_queries:
  - "rfi specification clarification"
time_analysis_enabled: false
```

Constraint: Only add keys that are valid EventTypeConfig fields. Do NOT use eval or exec anywhere near these files during loading.
  </action>
  <verify>
    <automated>python -c "
import yaml
from src.shared.models.config import EventTypeConfig
for name in ['schedule_update', 'rfi_request']:
    with open(f'config/event_types/{name}.yaml') as f:
        data = yaml.safe_load(f)
    cfg = EventTypeConfig(**data)
    assert cfg.event_type == name, f'event_type mismatch: {cfg.event_type!r} != {name!r}'
    print(f'{name}.yaml OK — signals: {cfg.allowed_signals}')
print('Both stub configs load cleanly.')
"</automated>
  </verify>
  <done>schedule_update.yaml and rfi_request.yaml each instantiate as valid EventTypeConfig objects with no ValidationError.</done>
</task>

</tasks>

<verification>
All three event type YAML configs load cleanly as EventTypeConfig objects:
```bash
python -c "
import yaml
from src.shared.models.config import EventTypeConfig
for name in ['material_change', 'schedule_update', 'rfi_request']:
    with open(f'config/event_types/{name}.yaml') as f:
        data = yaml.safe_load(f)
    cfg = EventTypeConfig(**data)
    print(f'{name}: event_type={cfg.event_type}, signals={cfg.allowed_signals}')
print('All 3 configs valid.')
"
```
</verification>

<success_criteria>
- material_change.yaml has 3 allowed_signals and 4 enrichment_queries, passes EventTypeConfig validation
- schedule_update.yaml and rfi_request.yaml both load without ValidationError
- No extra YAML keys that would break strict Pydantic parsing
</success_criteria>

<output>
After completion, create `.planning/phases/05-domain-processing/05-01-SUMMARY.md`
</output>
